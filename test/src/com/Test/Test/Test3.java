package com.Test.Test;

/*public class Test3 {
    public static void main(String[] args) {
        int[] arr = {2,1,5,3,8,4,9,7};
        for (int i = 0; i < arr.length - 1; i++) {
            for (int f = 0; f < arr.length - 1; f++) {
                if (arr[f] > arr[f + 1]) {
                    int a = arr[f + 1];
                    arr[f + 1] = arr[f];
                    arr[f] = a;
                }
            }
        }
        System.out.println(gt(arr, 9));
    }

    public static int gt(int[] arr, int a) {
        int b = 0;
        int c = arr.length;

        while (b <= c) {
            int i = (b + c) / 2;
            if (a == arr[i]) {
                return i;
            } else if (a > arr[i]) {
                b = i + 1;
            } else if (a < arr[i]) {
                c = i - 1;
            }
        }
        return -1;
    }
}*/
